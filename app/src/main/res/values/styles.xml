<resources>
    <!-- Din existerande tema eller stil -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Anpassade teman -->
    </style>

    <!-- Din anpassade knappstil -->
    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@null</item> <!-- Tar bort standard-tint -->
        <item name="shapeAppearanceOverlay">@style/CustomButtonShape</item>
    </style>

    <!-- Anpassad form för rundade hörn -->
    <style name="CustomButtonShape" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>
</resources>